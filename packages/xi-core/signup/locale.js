import React from 'react';
import { FormattedMessage } from 'react-intl';

export const ThreeDevices = () => (
  <FormattedMessage
    id="signin.threeDevices"
    defaultMessage="You can sign in to OTRO on as many devices as you like, but you only can watch content on a maximum of three."
  />
);
